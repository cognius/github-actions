name: Add host aliases
description: Create host aliases using /etc/hosts file
author: Kamontat Chantrachirathumrong

runs:
  using: "composite"
  steps:
    - name: create hosts
      run: $GITHUB_ACTION_PATH/add-hosts.sh
      shell: bash
